<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="w-full min-h-screen bg-zinc-800 flex justify-center items-center gap-10 flex-wrap"
  >
    <% data.forEach((item) => { %>
    <img
      class="w-96"
      src="data:<%= item.mimetype %>;base64,<%= item.image.toString('base64') %>"
      alt="Image"
    />
    <% }) %>
  </body>
</html>
